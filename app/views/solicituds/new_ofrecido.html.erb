<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CambiaLibros</title>
</head>
<body>
    <main>
        <%= render 'shared/header-chico' %>
        <section class="seccion-subtitulo">
            <h2 id="subtitulo">Por favor confirma tu solicitud de intercambio</h2>
        </section>
        <section class="seccion-subtitulo">
            <%= form_with model: @solicitud, url: solicituds_create_path do |form|%>
           
            <%= form.hidden_field :solicitable_type, value:@solicitable_type%>
          
            <%= form.hidden_field :solicitable_id, value:@solicitable_id%>
           
            <%= form.hidden_field :ofreciable_type, value:@ofreciable_type%>
            
            <%= form.hidden_field :ofreciable_id, value:@ofreciable_id%>
            
            <%= form.submit "Confirmar solicitud de intercambio"%>
            <% end %>
            
    </main>
    <footer>

    </footer>
</body>
</html>